# Cache Engine Configuration
cache:
    # Configure cache servers below
    stores:
        # Primary cache server (bundled within docker configuration)
        primary:
            driver: redis
            host: 10.0.11.3
            port: 6379
            timeout: 1
