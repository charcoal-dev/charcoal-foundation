# Databases
databases:
    # Primary database for application
    primary:
        driver: mysql
        host: 10.0.11.2
        port: 3306
        database: primary
        username: root
        password: ~
        # Connection Strategy
        # Possible values are ("lazy", "default")
        connection: onDemand